{% extends 'base.html.twig' %}

{% block title %}ImageCards{% endblock %}

{% block body %}
    <h1>Images</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_card.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ image_card.Image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('image_cards_index') }}">Retour à la liste</a>

    <a href="{{ path('image_cards_edit', {'id': image_card.id}) }}">Editer</a>

    {{ include('image_cards/_delete_form.html.twig') }}
{% endblock %}
