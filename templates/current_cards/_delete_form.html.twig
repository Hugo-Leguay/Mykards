<form method="post" action="{{ path('current_cards_delete', {'id': current_card.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ current_card.id) }}">
    <button class="btn">Supprimer</button>
</form>
